# ============================================
# NanoClaw 备份配置
# 复制到 .env 文件并填写你的配置
# ============================================

# --------------------------------------------
# SQLite 数据库备份到 Vercel Blob
# --------------------------------------------

# 启用 SQLite 备份
ENABLE_SQLITE_BACKUP=true

# 备份间隔（分钟，默认 60）
SQLITE_BACKUP_INTERVAL=60

# Vercel Blob Token（从 Vercel Dashboard 获取）
# https://vercel.com/dashboard/stores/blob
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# 加密密钥（32 字符以上，用于加密备份文件）
# 生成命令: openssl rand -hex 32
BACKUP_KEY=your_64_char_hex_key_here_32bytes_minimum_length

# --------------------------------------------
# 文件记忆备份到 GitHub
# --------------------------------------------

# Git 仓库地址（建议使用私有仓库）
# 格式: https://<token>@github.com/username/nanoclaw-memory.git
# 或使用 SSH: git@github.com:username/nanoclaw-memory.git
GIT_BACKUP_REPO=https://github.com/yourusername/nanoclaw-memory.git

# 分支名称（默认 main）
GIT_BACKUP_BRANCH=main

# Git 提交者信息
GIT_BACKUP_NAME=NanoClaw Backup
GIT_BACKUP_EMAIL=backup@nanoclaw.local

# 备份间隔（分钟，默认 30）
GIT_BACKUP_INTERVAL=30

# ============================================
# 推荐设置
# ============================================
#
# 1. 创建 GitHub 私有仓库: nanoclaw-memory
# 2. 生成 GitHub Personal Access Token:
#    https://github.com/settings/tokens
#    权限需要: repo
#
# 3. 设置环境变量:
#    export GIT_BACKUP_REPO="https://<token>@github.com/<user>/<repo>.git"
#
# 4. 初始化一次：
#    cd groups && git init && git remote add origin $GIT_BACKUP_REPO
#
# 5. 启动 NanoClaw，备份会自动运行
